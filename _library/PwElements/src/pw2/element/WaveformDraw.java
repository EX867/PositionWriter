package pw2.element;
import beads.Sample;
import kyui.util.Rect;
import processing.core.PGraphics;
public class WaveformDraw {
  public void render(PGraphics g, Rect pos, Sample sample, int color, float offsetX, float scale) {
    g.stroke(color);
    //#ADD
  }
}
